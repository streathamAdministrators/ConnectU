<h1>Listing users</h1>

<table>
  <tr>
    <th>Prenom</th>
    <th>Nom</th>
    <th>Mail</th>
    <th>Password</th>
    <th>Isvisible</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.prenom %></td>
    <td><%= user.nom %></td>
    <td><%= user.mail %></td>
    <td><%= user.password %></td>
    <td><%= user.isvisible %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>

<%= link_to 'Inviter quelqun ?', :controller => "mailer", :action => "index" %>
